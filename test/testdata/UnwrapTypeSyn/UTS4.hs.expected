module UTS4 where

type Bar = String

getInt :: (  Int,    String  ) -> Int
getInt (i, _) = i

getString :: (  Int,    String  ) -> String
getString (_, s) = s

addOne :: (  Int,    String  ) -> (  Int,    String  )
addOne (i, s) = (i+1, s)